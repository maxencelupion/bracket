info:
  name: Login pseudo
  type: http
  seq: 2

http:
  method: POST
  url: http://{{baseUrl}}:{{appPort}}/auth/login
  body:
    type: json
    data: |-
      {
        "pseudo": "pseudo",
        "password": "Password1!"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

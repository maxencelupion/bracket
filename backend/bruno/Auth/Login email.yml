info:
  name: Login email
  type: http
  seq: 3

http:
  method: POST
  url: http://{{baseUrl}}:{{appPort}}/auth/login
  body:
    type: json
    data: |-
      {
        "email": "email@gmail.com",
        "password": "Password1!"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

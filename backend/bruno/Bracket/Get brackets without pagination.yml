info:
  name: Get brackets without pagination
  type: http
  seq: 2

http:
  method: GET
  url: http://{{baseUrl}}:{{appPort}}/brackets
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
